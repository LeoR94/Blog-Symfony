<nav class="orange darken-1" role="navigation">
    <div class="nav-wrapper container">
        <a href="{{ path('blog_homepage') }}" class="brand-logo left">Mini Blog</a>
        <ul id="nav-mobile" class="right">
            <li> <a href="{{ path('blog_homepage') }} ">Accueil</a> </li>
            <li> <a href="{{ path('blog_articles') }} ">Articles</a> </li>
            {% if app.user %}
                <li>
                    <a href="{{ path('blog_administration')}}">Administration</a>
                </li>
                <li>
                    <a href="{{ path('blog_logout')}}" class="waves-effect waves-light btn">Se Déconnecter</a>
                </li>
                {% else %}
                    <li>
                        <a href="{{ path('blog_login')}}" class="waves-effect waves-light btn">Se Connecter</a>
                    </li>
            {% endif %}
    </div>
</nav>